<div class="col-md-6 col-md-offset-3">
  <div class="postimageclass" ><%= image_tag(@picture.image.url(:large), class: "img-responsive", style: "background-color:white;") if @picture.image? %></div>

  <%= link_to 'Back', root_path %>
</div>
